df_subset$Pest.prescence <- as.factor(df_subset$Pest.prescence.)
df_subset$Crop.variety <- as.factor(df_subset$Crop.variety)
str(df_subset)
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Crop.variety,
Pest.prescence.,
)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
df_subset$irrigation_applied <- as.factor(df_subset$Wheat.Type)
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Crop.variety,
Pest.prescence.,
)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
methods(mice)
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence <- as.factor(df_subset$Pest.prescence.)
df_subset$Crop.variety <- as.factor(df_subset$Crop.variety)
str(df_subset)
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Crop.variety,
Pest.prescence.,
)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
methods(mice)
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
df_subset$Crop.variety <- as.factor(df_subset$Crop.variety)
str(df_subset)
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(forcats)
df$Crop.variety <- fct_lump(df$Crop.variety, n = 20)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Crop.variety,
Pest.prescence.,
)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(forcats)
df$Crop.variety <- fct_lump(df$Crop.variety, n = 20)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Crop.variety,
Pest.prescence.,
)
# Count and sort frequencies of each variety
table_crop <- sort(table(df_subset$Crop.variety), decreasing = TRUE)
print(table_crop)
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(forcats)
df$Crop.variety <- fct_lump(df$Crop.variety, n = 20)
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(forcats)
df$Crop.variety <- fct_lump(df$Crop.variety, n = 20)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
methods(mice)
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
df_subset$Crop.variety <- as.factor(df_subset$Crop.variety)
str(df_subset)
imputed_data <- mice(df_subset, m=5, method = "rf")
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
methods(mice)
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
str(df_subset)
imputed_data <- mice(df_subset, m=5, method = "rf")
View(imputed_data)
completed_data <- complete(imputed_data, action = 1)
completed_data <- complete(imputed_data, action = 1)
str(completed_data)
summary(completed_data)
colSums(is.na(completed_data))  # confirm no NAs
#see the outputs
summary(imputed_data)
imputed_data$imp$N_rate
summary(completed_data)
str(completed_data)
summary(completed_data)
summary(df_subset)
#see the outputs
summary(imputed_data)
summary(completed_data)
#save dataset
write.csv(imputed_data, "../../../data/finaldatasets/testdata/imputation/imputed_dataset.csv", row.names = FALSE)
#save dataset
write.csv(completed_data, "../../../data/finaldatasets/testdata/imputation/imputed_dataset.csv", row.names = FALSE)
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
setwd("~/Documents/FinalProject/code/analysis/kagglefeatureengineering")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
year
)
summary(df_subset)
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
View(df_subset)
methods(mice)
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
str(df_subset)
imputed_data <- mice(df_subset, m=1, method = "rf")
completed_data <- complete(imputed_data, action = 1)
str(completed_data)
summary(completed_data)
colSums(is.na(completed_data))  # confirm no NAs
#see the outputs
summary(imputed_data)
imputed_data$imp$N_rate
#see if imputation worked
plot(density(df_subset$N_rate, na.rm = TRUE))
lines(density(imputed_data$imp$N_rate), col = "red", lty =3, lwd = 2)
#save dataset
write.csv(completed_data, "../../../data/finaldatasets/testdata/imputation/imputed_dataset.csv", row.names = FALSE)
setwd("~/Documents/FinalProject/code/analysis/kagglefeatureengineering")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/Treedata1.csv")
View(df_raw)
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
year,
gdp
)
View(df_subset)
View(df_raw)
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
year,
gdp_per_capita
)
summary(df_subset)
par(cex =0.5)
md.pattern(df_subset)
# Compute correlation matrix
cor_matrix <- cor(df_subset, use = "complete.obs", method = "pearson")
#Define features as factors for corr matrix
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
# Compute correlation matrix
cor_matrix <- cor(df_subset, use = "complete.obs", method = "pearson", )
#choose only numeric values
numeric_df <- df_subset[, sapply(df, is.numeric)]
#choose only numeric values
numeric_df <- df_subset[, sapply(df_subset, is.numeric)]
# Compute correlation matrix
cor_matrix <- cor(df_subset, use = "complete.obs", method = "pearson", )
View(numeric_df)
View(numeric_df)
#drop coords and year
numeric_df <- numeric_df[, !(names(numeric_df) %in% c("lon", "lat", "year"))]
# Compute correlation matrix
cor_matrix <- cor(df_subset, use = "complete.obs", method = "pearson", )
# Compute correlation matrix
cor_matrix <- cor(df_subset, use = "pairwise.complete.obs", method = "pearson", )
# View it
print(cor_matrix)
# Compute correlation matrix
cor_matrix <- cor(numeric_df, use = "pairwise.complete.obs", method = "pearson", )
# View it
print(cor_matrix)
install.packages("corrplot")
library(corrplot)
corrplot(cor_matrix, method = "color", type = "upper",
tl.col = "black", tl.srt = 45, addCoef.col = "black")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(corrplot)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/Treedata1.csv")
setwd("~/Documents/FinalProject/code/analysis/kagglefeatureengineering")
# --- Load Packages ---
library(missForest)
library(dplyr)
library(readr)
library(mice)
library(corrplot)
# --- Set Global Options ---
options(digits = 10, scipen = 999)
# --- Load Dataset ---
df_raw <- read_csv("../../../data/finaldatasets/testdata/XGBoost.csv")
# --- Select Relevant Columns (no 'year') ---
df_subset <- df_raw %>%
select(
lon = Conversion.for.longitude,
lat = Conversion.for.latitude,
yield = `Grain.yield..tons.ha.1.`,
N_rate = `N.rate..kg.N.ha.1.`,
starts_with("temp"),
starts_with("prc"),
AEZ,
Soil_N,
Soil.pH,
Soil.organic.carbon..g.C.kg.1.,
Sand,
Silt,
Elevation,
pr_irrigated,
Wheat.Type,
Pest.prescence.,
year,
gdp_per_capita
)
summary(df_subset)
#assess missingness
par(cex =0.5)
md.pattern(df_subset)
par(cex = 1)
#choose only numeric values
numeric_df <- df_subset[, sapply(df_subset, is.numeric)]
#drop coords and year
numeric_df <- numeric_df[, !(names(numeric_df) %in% c("lon", "lat", "year"))]
# Compute correlation matrix
cor_matrix <- cor(numeric_df, use = "pairwise.complete.obs", method = "pearson", )
# View it
print(cor_matrix)
corrplot(cor_matrix, method = "color", type = "upper",
tl.col = "black", tl.srt = 45, addCoef.col = "black")
df_subset$Wheat.Type <- as.factor(df_subset$Wheat.Type)
df_subset$Pest.prescence. <- as.factor(df_subset$Pest.prescence.)
str(df_subset)
imputed_data <- mice(df_subset, m=1, method = "rf")
completed_data <- complete(imputed_data, action = 1)
View(completed_data)
str(completed_data)
summary(completed_data)
colSums(is.na(completed_data))  # confirm no NAs
#see the outputs
summary(imputed_data)
#assess missingness
par(cex =0.5)
md.pattern(completed_data)
par(cex = 1)
#see the outputs
summary(imputed_data)
#see the outputs
summary(imputed_data)
imputed_data$imp$N_rate
#save dataset
write.csv(completed_data, "../../../data/finaldatasets/testdata/imputation/imputed_dataset.csv", row.names = FALSE)
source("~/Documents/FinalProject/code/analysis/kagglefeatureengineering/Resolutioncheck.R")
file_names <- c(
"aez_v9v2red_5m_CRUTS32_Hist_8110_100_avg.tif",
"clay_0_30cm.tif",
"elevation_world.tif",
"irrigated_gmia_v5_aei_pct.asc",
"phh2o_0_30cm.tif",
"sand_0_30cm.tif",
"silt_0_30cm.tif",
"soc_0_30cm.tif",
"soil_nitrogen_0_30cm.tif",
"rast_adm2_gdp_perCapita_1990_2022.tif"
)
source("~/Documents/FinalProject/code/analysis/kagglefeatureengineering/Resolutioncheck.R")
source("~/Documents/FinalProject/code/analysis/kagglefeatureengineering/Resolutioncheck.R")
source("~/Documents/FinalProject/code/analysis/kagglefeatureengineering/Resolutioncheck.R")
